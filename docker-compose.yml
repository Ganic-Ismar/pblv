version: '3.8'
services:
  database:
    build:
      context: ./database
      dockerfile: Dockerfile
    environment:
      env_file:
        .env
    ports:
      - 5432:5432
    volumes:
      - ./database/data:/var/lib/postgresql/data